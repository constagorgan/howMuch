/*global define, console */
define([
  "jquery",
  "underscore",
  "backbone",
  "ws",
], function ($, _, Backbone, ws) {
  "use strict";
  
  var crawler = {}
  
  crawler.buildCrawler = function() {
    console.log('hau bau')
  }
  
  return crawler
});
